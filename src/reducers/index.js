import authReducer, { login, logout } from "./authSlice";
import themeReducer, {toggleTheme} from "./themSlice";


export {
    authReducer,
    login,
    logout,
    themeReducer,
    toggleTheme,
}